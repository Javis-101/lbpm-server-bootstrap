# v1.0.3 Validation Evidence Disposition

## Status

```text
ARTIFACT_IDENTITY=PASS
V1_0_3_GPU_VALIDATION_EVIDENCE=NOT_FOUND
PUBLIC_RELEASE_ARTIFACT=BLOCKED
OVERALL=NOT_READY
```

The original `LBPM-server-bootstrap-v1.0.3.zip` was found locally at 48,477,391 bytes and its SHA256 matches the frozen value:

```text
f1bf6e5649769aba5d246535d3f74f1cbc4032ebab78ee9bb54fa7a439360507
```

The artifact was not modified or repacked during publication work.

The immutable ZIP also contains unredacted historical validation records with a hostname, run identifiers, `/root/...` server paths, and the original research RAW filename/hash. Those records contain no detected credential, private key, or research RAW bytes, but they are not suitable for a public release asset. Because v1.0.3 cannot be sanitized without changing its bytes, this privacy finding independently blocks publication of the original ZIP.

## Available evidence

The frozen ZIP contains no v1.0.3 fresh-install or compatible-reuse PASS record. Its README and changelog state `REAL GPU RETEST NOT YET EXECUTED`.

The available records establish only:

- a historical RTX 2080 Ti run using Portable Installer 2.0.5-offline and SOP 1.3.0, where SOP acceptance and one bounded real-rock smoke passed;
- a v1.0.0 server failure caused by executable-bit assumptions and fail-open orchestration;
- a v1.0.1 pre-retest audit that was not promoted to real-server validation;
- a v1.0.2 Tesla T4 / Ubuntu 24.04.4 x86_64 run with CUDA 12.8 and GCC 13.3.0 that failed during installation because the host lacked usable GNU Fortran linkage.

After the operator repaired GNU Fortran, the prerequisite link probe passed, but no included record proves that v1.0.3 subsequently completed fresh installation, exact compatible reuse, post-install acceptance, RAW/Z-connectivity/ROI gates, GPU ColorModel smoke, or `READY_FOR_PARAMETERIZATION`.

Hostnames, run IDs, private absolute paths, evidence-archive hashes, and the original research RAW filename/hash are intentionally omitted from this public record. Where a filename is needed in examples, use `validation_rock_001.raw`.

## Gate table

| Gate | Public evidence result |
| --- | --- |
| Artifact SHA256 | PASS |
| Fresh install, v1.0.3 | NOT VERIFIED |
| Compatible reuse, v1.0.3 | NOT VERIFIED |
| LBPM identity, CUDA runtime, Piston/basic tests | historical older-component PASS only |
| RAW contract, Z connectivity, source immutable, ROI preserved | historical older-component PASS only |
| GPU ColorModel smoke | historical older-component PASS only |
| Evidence workflow, v1.0.3 | NOT VERIFIED |
| Overall v1.0.3 production status | BLOCKED |

## Scientific boundary

Passing Bootstrap/SOP validation means the infrastructure, LBPM build, GPU runtime, data contract, and smoke simulation are engineering-ready.

It does **not** constitute physical validation of a production water-displacing-gas simulation, residual gas saturation, capillary-number selection, wettability model, or final scientific parameterization.

## Required closure evidence

A new safety-patched version must complete one fresh installation and a second exact compatible-reuse run on a supported Linux NVIDIA GPU host. The sanitized record must be derived from the resulting immutable evidence and must include OS/architecture, GPU/compute capability, CUDA, compiler and GNU Fortran linkage, OpenMPI, HDF5, LBPM commit, bootstrap/component versions, all stage results, and final artifact hashes.
