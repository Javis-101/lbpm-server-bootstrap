# LBPM post-install SOP v1.3.2 configuration.
# Copy to config.env only when overriding defaults.

STACK_DIR=/root/LBPM-stack
DATA_DIR=/root/rivermind-data
VALIDATION_DIR=/root/rivermind-data/lbpm-validation
SIMULATION_DIR=/root/rivermind-data/lbpm-simulations
MPI_RANKS=1
